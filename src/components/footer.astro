---
import { Icon } from "astro-icon/components";
import type { FooterLink } from "~/types";

const links: Array<FooterLink> = [
  {
    url: "https://www.instagram.com/vri.admonyserv",
    description: "VRI Instagram",
    icon: "fa-brands:instagram",
  },
  {
    url: "https://www.facebook.com/vrisys",
    description: "VRI Facebook",
    icon: "fa-brands:facebook",
  },
];
---

<footer
  class="flex h-64 items-center justify-center bg-opacity-90 bg-footer bg-cover"
>
  <ul class="flex justify-center items-center gap-4">
    {
      links.map((link) => (
        <li>
          <a
            target="_blank"
            class="text-white hover:text-primary flex size-16 items-center justify-center rounded-full border-4 border-current p-4"
            href={link.url}
          >
            <span class="sr-only">{link.description}</span>
            <Icon class="size-full" name={link.icon} />
          </a>
        </li>
      ))
    }
  </ul>
</footer>

<style>
  .bg-footer {
    background-image: url("/footerbg.webp");
  }
</style>
